{
  "name": "hungry_bird",
  "private": true,
  "scripts": {
    "lint": "cd client && yarn run lint",
    "postinstall": "cd client && yarn install",
    "test": "cd client && yarn run test",
    "test:coverage": "cd client && yarn run test:coverage",
    "test:watch": "cd client && yarn run test:watch"
  },
  "devDependencies": {
    "webpack": "^4.20.2",
    "webpack-dev-server": "^3.2.1",
    "webpack-merge": "^4.2.1"
  }
}